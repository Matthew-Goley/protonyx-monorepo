# Shared secret that callers must send in X-API-Key header
LENS_API_KEY="change-me-to-a-long-random-string"

# Writable directory for market data cache (market_data.json) and Lens history
# Defaults to ~/Vector/data on Linux if unset
LENS_DATA_DIR=/tmp/lens_data
